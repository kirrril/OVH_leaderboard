using System;
using UnityEngine;

public class Desk : MonoBehaviour
{
    // [SerializeField]
    // private Transform playerPosition;
    // public event Action SatDownAtTheDesk;

    // void OnTriggerEnter(Collider other)
    // {
    //     if (!other.CompareTag("Player")) return;

    //     PlayerStates.isGaming = true;
    //     other.gameObject.GetComponent<Rigidbody>().isKinematic = true;
    //     other.gameObject.transform.position = playerPosition.position;
    //     other.gameObject.transform.rotation = playerPosition.rotation;

    //     SatDownAtTheDesk.Invoke();
    // }
}
